// Code generated by ent, DO NOT EDIT.

package ent

import (
	"context"
	"fmt"
	"math"

	"entgo.io/ent"
	"entgo.io/ent/dialect/sql"
	"entgo.io/ent/dialect/sql/sqlgraph"
	"entgo.io/ent/schema/field"
	"github.com/espitman/jbm-hr-backend/ent/digikalacode"
	"github.com/espitman/jbm-hr-backend/ent/predicate"
	"github.com/espitman/jbm-hr-backend/ent/user"
)

// DigikalaCodeQuery is the builder for querying DigikalaCode entities.
type DigikalaCodeQuery struct {
	config
	ctx        *QueryContext
	order      []digikalacode.OrderOption
	inters     []Interceptor
	predicates []predicate.DigikalaCode
	withUsedBy *UserQuery
	// intermediate query (i.e. traversal path).
	sql  *sql.Selector
	path func(context.Context) (*sql.Selector, error)
}

// Where adds a new predicate for the DigikalaCodeQuery builder.
func (dcq *DigikalaCodeQuery) Where(ps ...predicate.DigikalaCode) *DigikalaCodeQuery {
	dcq.predicates = append(dcq.predicates, ps...)
	return dcq
}

// Limit the number of records to be returned by this query.
func (dcq *DigikalaCodeQuery) Limit(limit int) *DigikalaCodeQuery {
	dcq.ctx.Limit = &limit
	return dcq
}

// Offset to start from.
func (dcq *DigikalaCodeQuery) Offset(offset int) *DigikalaCodeQuery {
	dcq.ctx.Offset = &offset
	return dcq
}

// Unique configures the query builder to filter duplicate records on query.
// By default, unique is set to true, and can be disabled using this method.
func (dcq *DigikalaCodeQuery) Unique(unique bool) *DigikalaCodeQuery {
	dcq.ctx.Unique = &unique
	return dcq
}

// Order specifies how the records should be ordered.
func (dcq *DigikalaCodeQuery) Order(o ...digikalacode.OrderOption) *DigikalaCodeQuery {
	dcq.order = append(dcq.order, o...)
	return dcq
}

// QueryUsedBy chains the current query on the "used_by" edge.
func (dcq *DigikalaCodeQuery) QueryUsedBy() *UserQuery {
	query := (&UserClient{config: dcq.config}).Query()
	query.path = func(ctx context.Context) (fromU *sql.Selector, err error) {
		if err := dcq.prepareQuery(ctx); err != nil {
			return nil, err
		}
		selector := dcq.sqlQuery(ctx)
		if err := selector.Err(); err != nil {
			return nil, err
		}
		step := sqlgraph.NewStep(
			sqlgraph.From(digikalacode.Table, digikalacode.FieldID, selector),
			sqlgraph.To(user.Table, user.FieldID),
			sqlgraph.Edge(sqlgraph.M2O, true, digikalacode.UsedByTable, digikalacode.UsedByColumn),
		)
		fromU = sqlgraph.SetNeighbors(dcq.driver.Dialect(), step)
		return fromU, nil
	}
	return query
}

// First returns the first DigikalaCode entity from the query.
// Returns a *NotFoundError when no DigikalaCode was found.
func (dcq *DigikalaCodeQuery) First(ctx context.Context) (*DigikalaCode, error) {
	nodes, err := dcq.Limit(1).All(setContextOp(ctx, dcq.ctx, ent.OpQueryFirst))
	if err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nil, &NotFoundError{digikalacode.Label}
	}
	return nodes[0], nil
}

// FirstX is like First, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) FirstX(ctx context.Context) *DigikalaCode {
	node, err := dcq.First(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return node
}

// FirstID returns the first DigikalaCode ID from the query.
// Returns a *NotFoundError when no DigikalaCode ID was found.
func (dcq *DigikalaCodeQuery) FirstID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dcq.Limit(1).IDs(setContextOp(ctx, dcq.ctx, ent.OpQueryFirstID)); err != nil {
		return
	}
	if len(ids) == 0 {
		err = &NotFoundError{digikalacode.Label}
		return
	}
	return ids[0], nil
}

// FirstIDX is like FirstID, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) FirstIDX(ctx context.Context) int {
	id, err := dcq.FirstID(ctx)
	if err != nil && !IsNotFound(err) {
		panic(err)
	}
	return id
}

// Only returns a single DigikalaCode entity found by the query, ensuring it only returns one.
// Returns a *NotSingularError when more than one DigikalaCode entity is found.
// Returns a *NotFoundError when no DigikalaCode entities are found.
func (dcq *DigikalaCodeQuery) Only(ctx context.Context) (*DigikalaCode, error) {
	nodes, err := dcq.Limit(2).All(setContextOp(ctx, dcq.ctx, ent.OpQueryOnly))
	if err != nil {
		return nil, err
	}
	switch len(nodes) {
	case 1:
		return nodes[0], nil
	case 0:
		return nil, &NotFoundError{digikalacode.Label}
	default:
		return nil, &NotSingularError{digikalacode.Label}
	}
}

// OnlyX is like Only, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) OnlyX(ctx context.Context) *DigikalaCode {
	node, err := dcq.Only(ctx)
	if err != nil {
		panic(err)
	}
	return node
}

// OnlyID is like Only, but returns the only DigikalaCode ID in the query.
// Returns a *NotSingularError when more than one DigikalaCode ID is found.
// Returns a *NotFoundError when no entities are found.
func (dcq *DigikalaCodeQuery) OnlyID(ctx context.Context) (id int, err error) {
	var ids []int
	if ids, err = dcq.Limit(2).IDs(setContextOp(ctx, dcq.ctx, ent.OpQueryOnlyID)); err != nil {
		return
	}
	switch len(ids) {
	case 1:
		id = ids[0]
	case 0:
		err = &NotFoundError{digikalacode.Label}
	default:
		err = &NotSingularError{digikalacode.Label}
	}
	return
}

// OnlyIDX is like OnlyID, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) OnlyIDX(ctx context.Context) int {
	id, err := dcq.OnlyID(ctx)
	if err != nil {
		panic(err)
	}
	return id
}

// All executes the query and returns a list of DigikalaCodes.
func (dcq *DigikalaCodeQuery) All(ctx context.Context) ([]*DigikalaCode, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryAll)
	if err := dcq.prepareQuery(ctx); err != nil {
		return nil, err
	}
	qr := querierAll[[]*DigikalaCode, *DigikalaCodeQuery]()
	return withInterceptors[[]*DigikalaCode](ctx, dcq, qr, dcq.inters)
}

// AllX is like All, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) AllX(ctx context.Context) []*DigikalaCode {
	nodes, err := dcq.All(ctx)
	if err != nil {
		panic(err)
	}
	return nodes
}

// IDs executes the query and returns a list of DigikalaCode IDs.
func (dcq *DigikalaCodeQuery) IDs(ctx context.Context) (ids []int, err error) {
	if dcq.ctx.Unique == nil && dcq.path != nil {
		dcq.Unique(true)
	}
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryIDs)
	if err = dcq.Select(digikalacode.FieldID).Scan(ctx, &ids); err != nil {
		return nil, err
	}
	return ids, nil
}

// IDsX is like IDs, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) IDsX(ctx context.Context) []int {
	ids, err := dcq.IDs(ctx)
	if err != nil {
		panic(err)
	}
	return ids
}

// Count returns the count of the given query.
func (dcq *DigikalaCodeQuery) Count(ctx context.Context) (int, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryCount)
	if err := dcq.prepareQuery(ctx); err != nil {
		return 0, err
	}
	return withInterceptors[int](ctx, dcq, querierCount[*DigikalaCodeQuery](), dcq.inters)
}

// CountX is like Count, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) CountX(ctx context.Context) int {
	count, err := dcq.Count(ctx)
	if err != nil {
		panic(err)
	}
	return count
}

// Exist returns true if the query has elements in the graph.
func (dcq *DigikalaCodeQuery) Exist(ctx context.Context) (bool, error) {
	ctx = setContextOp(ctx, dcq.ctx, ent.OpQueryExist)
	switch _, err := dcq.FirstID(ctx); {
	case IsNotFound(err):
		return false, nil
	case err != nil:
		return false, fmt.Errorf("ent: check existence: %w", err)
	default:
		return true, nil
	}
}

// ExistX is like Exist, but panics if an error occurs.
func (dcq *DigikalaCodeQuery) ExistX(ctx context.Context) bool {
	exist, err := dcq.Exist(ctx)
	if err != nil {
		panic(err)
	}
	return exist
}

// Clone returns a duplicate of the DigikalaCodeQuery builder, including all associated steps. It can be
// used to prepare common query builders and use them differently after the clone is made.
func (dcq *DigikalaCodeQuery) Clone() *DigikalaCodeQuery {
	if dcq == nil {
		return nil
	}
	return &DigikalaCodeQuery{
		config:     dcq.config,
		ctx:        dcq.ctx.Clone(),
		order:      append([]digikalacode.OrderOption{}, dcq.order...),
		inters:     append([]Interceptor{}, dcq.inters...),
		predicates: append([]predicate.DigikalaCode{}, dcq.predicates...),
		withUsedBy: dcq.withUsedBy.Clone(),
		// clone intermediate query.
		sql:  dcq.sql.Clone(),
		path: dcq.path,
	}
}

// WithUsedBy tells the query-builder to eager-load the nodes that are connected to
// the "used_by" edge. The optional arguments are used to configure the query builder of the edge.
func (dcq *DigikalaCodeQuery) WithUsedBy(opts ...func(*UserQuery)) *DigikalaCodeQuery {
	query := (&UserClient{config: dcq.config}).Query()
	for _, opt := range opts {
		opt(query)
	}
	dcq.withUsedBy = query
	return dcq
}

// GroupBy is used to group vertices by one or more fields/columns.
// It is often used with aggregate functions, like: count, max, mean, min, sum.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//		Count int `json:"count,omitempty"`
//	}
//
//	client.DigikalaCode.Query().
//		GroupBy(digikalacode.FieldCode).
//		Aggregate(ent.Count()).
//		Scan(ctx, &v)
func (dcq *DigikalaCodeQuery) GroupBy(field string, fields ...string) *DigikalaCodeGroupBy {
	dcq.ctx.Fields = append([]string{field}, fields...)
	grbuild := &DigikalaCodeGroupBy{build: dcq}
	grbuild.flds = &dcq.ctx.Fields
	grbuild.label = digikalacode.Label
	grbuild.scan = grbuild.Scan
	return grbuild
}

// Select allows the selection one or more fields/columns for the given query,
// instead of selecting all fields in the entity.
//
// Example:
//
//	var v []struct {
//		Code string `json:"code,omitempty"`
//	}
//
//	client.DigikalaCode.Query().
//		Select(digikalacode.FieldCode).
//		Scan(ctx, &v)
func (dcq *DigikalaCodeQuery) Select(fields ...string) *DigikalaCodeSelect {
	dcq.ctx.Fields = append(dcq.ctx.Fields, fields...)
	sbuild := &DigikalaCodeSelect{DigikalaCodeQuery: dcq}
	sbuild.label = digikalacode.Label
	sbuild.flds, sbuild.scan = &dcq.ctx.Fields, sbuild.Scan
	return sbuild
}

// Aggregate returns a DigikalaCodeSelect configured with the given aggregations.
func (dcq *DigikalaCodeQuery) Aggregate(fns ...AggregateFunc) *DigikalaCodeSelect {
	return dcq.Select().Aggregate(fns...)
}

func (dcq *DigikalaCodeQuery) prepareQuery(ctx context.Context) error {
	for _, inter := range dcq.inters {
		if inter == nil {
			return fmt.Errorf("ent: uninitialized interceptor (forgotten import ent/runtime?)")
		}
		if trv, ok := inter.(Traverser); ok {
			if err := trv.Traverse(ctx, dcq); err != nil {
				return err
			}
		}
	}
	for _, f := range dcq.ctx.Fields {
		if !digikalacode.ValidColumn(f) {
			return &ValidationError{Name: f, err: fmt.Errorf("ent: invalid field %q for query", f)}
		}
	}
	if dcq.path != nil {
		prev, err := dcq.path(ctx)
		if err != nil {
			return err
		}
		dcq.sql = prev
	}
	return nil
}

func (dcq *DigikalaCodeQuery) sqlAll(ctx context.Context, hooks ...queryHook) ([]*DigikalaCode, error) {
	var (
		nodes       = []*DigikalaCode{}
		_spec       = dcq.querySpec()
		loadedTypes = [1]bool{
			dcq.withUsedBy != nil,
		}
	)
	_spec.ScanValues = func(columns []string) ([]any, error) {
		return (*DigikalaCode).scanValues(nil, columns)
	}
	_spec.Assign = func(columns []string, values []any) error {
		node := &DigikalaCode{config: dcq.config}
		nodes = append(nodes, node)
		node.Edges.loadedTypes = loadedTypes
		return node.assignValues(columns, values)
	}
	for i := range hooks {
		hooks[i](ctx, _spec)
	}
	if err := sqlgraph.QueryNodes(ctx, dcq.driver, _spec); err != nil {
		return nil, err
	}
	if len(nodes) == 0 {
		return nodes, nil
	}
	if query := dcq.withUsedBy; query != nil {
		if err := dcq.loadUsedBy(ctx, query, nodes, nil,
			func(n *DigikalaCode, e *User) { n.Edges.UsedBy = e }); err != nil {
			return nil, err
		}
	}
	return nodes, nil
}

func (dcq *DigikalaCodeQuery) loadUsedBy(ctx context.Context, query *UserQuery, nodes []*DigikalaCode, init func(*DigikalaCode), assign func(*DigikalaCode, *User)) error {
	ids := make([]int, 0, len(nodes))
	nodeids := make(map[int][]*DigikalaCode)
	for i := range nodes {
		fk := nodes[i].UsedByUserID
		if _, ok := nodeids[fk]; !ok {
			ids = append(ids, fk)
		}
		nodeids[fk] = append(nodeids[fk], nodes[i])
	}
	if len(ids) == 0 {
		return nil
	}
	query.Where(user.IDIn(ids...))
	neighbors, err := query.All(ctx)
	if err != nil {
		return err
	}
	for _, n := range neighbors {
		nodes, ok := nodeids[n.ID]
		if !ok {
			return fmt.Errorf(`unexpected foreign-key "used_by_user_id" returned %v`, n.ID)
		}
		for i := range nodes {
			assign(nodes[i], n)
		}
	}
	return nil
}

func (dcq *DigikalaCodeQuery) sqlCount(ctx context.Context) (int, error) {
	_spec := dcq.querySpec()
	_spec.Node.Columns = dcq.ctx.Fields
	if len(dcq.ctx.Fields) > 0 {
		_spec.Unique = dcq.ctx.Unique != nil && *dcq.ctx.Unique
	}
	return sqlgraph.CountNodes(ctx, dcq.driver, _spec)
}

func (dcq *DigikalaCodeQuery) querySpec() *sqlgraph.QuerySpec {
	_spec := sqlgraph.NewQuerySpec(digikalacode.Table, digikalacode.Columns, sqlgraph.NewFieldSpec(digikalacode.FieldID, field.TypeInt))
	_spec.From = dcq.sql
	if unique := dcq.ctx.Unique; unique != nil {
		_spec.Unique = *unique
	} else if dcq.path != nil {
		_spec.Unique = true
	}
	if fields := dcq.ctx.Fields; len(fields) > 0 {
		_spec.Node.Columns = make([]string, 0, len(fields))
		_spec.Node.Columns = append(_spec.Node.Columns, digikalacode.FieldID)
		for i := range fields {
			if fields[i] != digikalacode.FieldID {
				_spec.Node.Columns = append(_spec.Node.Columns, fields[i])
			}
		}
		if dcq.withUsedBy != nil {
			_spec.Node.AddColumnOnce(digikalacode.FieldUsedByUserID)
		}
	}
	if ps := dcq.predicates; len(ps) > 0 {
		_spec.Predicate = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	if limit := dcq.ctx.Limit; limit != nil {
		_spec.Limit = *limit
	}
	if offset := dcq.ctx.Offset; offset != nil {
		_spec.Offset = *offset
	}
	if ps := dcq.order; len(ps) > 0 {
		_spec.Order = func(selector *sql.Selector) {
			for i := range ps {
				ps[i](selector)
			}
		}
	}
	return _spec
}

func (dcq *DigikalaCodeQuery) sqlQuery(ctx context.Context) *sql.Selector {
	builder := sql.Dialect(dcq.driver.Dialect())
	t1 := builder.Table(digikalacode.Table)
	columns := dcq.ctx.Fields
	if len(columns) == 0 {
		columns = digikalacode.Columns
	}
	selector := builder.Select(t1.Columns(columns...)...).From(t1)
	if dcq.sql != nil {
		selector = dcq.sql
		selector.Select(selector.Columns(columns...)...)
	}
	if dcq.ctx.Unique != nil && *dcq.ctx.Unique {
		selector.Distinct()
	}
	for _, p := range dcq.predicates {
		p(selector)
	}
	for _, p := range dcq.order {
		p(selector)
	}
	if offset := dcq.ctx.Offset; offset != nil {
		// limit is mandatory for offset clause. We start
		// with default value, and override it below if needed.
		selector.Offset(*offset).Limit(math.MaxInt32)
	}
	if limit := dcq.ctx.Limit; limit != nil {
		selector.Limit(*limit)
	}
	return selector
}

// DigikalaCodeGroupBy is the group-by builder for DigikalaCode entities.
type DigikalaCodeGroupBy struct {
	selector
	build *DigikalaCodeQuery
}

// Aggregate adds the given aggregation functions to the group-by query.
func (dcgb *DigikalaCodeGroupBy) Aggregate(fns ...AggregateFunc) *DigikalaCodeGroupBy {
	dcgb.fns = append(dcgb.fns, fns...)
	return dcgb
}

// Scan applies the selector query and scans the result into the given value.
func (dcgb *DigikalaCodeGroupBy) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dcgb.build.ctx, ent.OpQueryGroupBy)
	if err := dcgb.build.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DigikalaCodeQuery, *DigikalaCodeGroupBy](ctx, dcgb.build, dcgb, dcgb.build.inters, v)
}

func (dcgb *DigikalaCodeGroupBy) sqlScan(ctx context.Context, root *DigikalaCodeQuery, v any) error {
	selector := root.sqlQuery(ctx).Select()
	aggregation := make([]string, 0, len(dcgb.fns))
	for _, fn := range dcgb.fns {
		aggregation = append(aggregation, fn(selector))
	}
	if len(selector.SelectedColumns()) == 0 {
		columns := make([]string, 0, len(*dcgb.flds)+len(dcgb.fns))
		for _, f := range *dcgb.flds {
			columns = append(columns, selector.C(f))
		}
		columns = append(columns, aggregation...)
		selector.Select(columns...)
	}
	selector.GroupBy(selector.Columns(*dcgb.flds...)...)
	if err := selector.Err(); err != nil {
		return err
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dcgb.build.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}

// DigikalaCodeSelect is the builder for selecting fields of DigikalaCode entities.
type DigikalaCodeSelect struct {
	*DigikalaCodeQuery
	selector
}

// Aggregate adds the given aggregation functions to the selector query.
func (dcs *DigikalaCodeSelect) Aggregate(fns ...AggregateFunc) *DigikalaCodeSelect {
	dcs.fns = append(dcs.fns, fns...)
	return dcs
}

// Scan applies the selector query and scans the result into the given value.
func (dcs *DigikalaCodeSelect) Scan(ctx context.Context, v any) error {
	ctx = setContextOp(ctx, dcs.ctx, ent.OpQuerySelect)
	if err := dcs.prepareQuery(ctx); err != nil {
		return err
	}
	return scanWithInterceptors[*DigikalaCodeQuery, *DigikalaCodeSelect](ctx, dcs.DigikalaCodeQuery, dcs, dcs.inters, v)
}

func (dcs *DigikalaCodeSelect) sqlScan(ctx context.Context, root *DigikalaCodeQuery, v any) error {
	selector := root.sqlQuery(ctx)
	aggregation := make([]string, 0, len(dcs.fns))
	for _, fn := range dcs.fns {
		aggregation = append(aggregation, fn(selector))
	}
	switch n := len(*dcs.selector.flds); {
	case n == 0 && len(aggregation) > 0:
		selector.Select(aggregation...)
	case n != 0 && len(aggregation) > 0:
		selector.AppendSelect(aggregation...)
	}
	rows := &sql.Rows{}
	query, args := selector.Query()
	if err := dcs.driver.Query(ctx, query, args, rows); err != nil {
		return err
	}
	defer rows.Close()
	return sql.ScanSlice(rows, v)
}
